import os
class GameConstants:
	BRICK_SIZE = (100, 30)

	WIDTH = 800
	HEIGHT = 600
	SCREEN_SIZE = (WIDTH, HEIGHT)

	BALL_SIZE = [16,16]

	PAD_SIZE = [139, 13]

	# Colors
	BLACK = (0, 0 , 0)
	WHITE = (255, 255, 255)

	SPRITE_BALL = os.path.join("Assets", "Ball.png")